<%= form_for(@rate) do |f| %>
  <% if @rate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rate.errors.count, "error") %> prohibited this rate from being saved:</h2>

      <ul>
      <% @rate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%= f.hidden_field :product_id, :value => @rate.product_id %>

<%= f.hidden_field :user_id, :value => @rate.user_id %>

  
  <div class="field">
    <%= f.label :rating %><br />
    <%= f.select :rating,[0,1,2,3,4,5],
                  prompt: 'Enter a rating' %>
  </div>
  <div class="field">
    <%= f.label :review %><br />
    <%= f.text_area :review %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
